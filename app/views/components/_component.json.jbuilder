json.extract! component, :id, :name, :code, :description, :budget, :color, :status, :flag, :parent_id, :user_id, :project_id, :created_at, :updated_at
json.url component_url(component, format: :json)
