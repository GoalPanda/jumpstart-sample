<%= content_for :title, "Projects" %>
<%= turbo_stream_from :projects %>

<div class="container px-4 mx-auto my-8">
  <div class="flex items-center justify-between mb-4">
    <div>
      <h1 class="h3">Projects</h1>
      <div class="my-6">
        <p><%= Component.count(@project) %> Project Components </p>
        <p><%= Activity.all.count(@project) %> Component Activities </p>
      </div>

      <!-- <div class="text-sm font-medium">
        <span class="text-gray-500">Sort by:</span>
        <%# sortable @projects, :project_id, "Project", class: "text-gray-700 mr-1" %>
        <%# sortable @projects, :identificator, "Identificator", class: "text-gray-700 mr-1" %>
        <%# sortable @projects, :budget, "Budget", class: "text-gray-700 mr-1" %>
        <%# sortable @projects, :category, "Category", class: "text-gray-700 mr-1" %>
        <%# sortable @projects, :classification, "Classification", class: "text-gray-700 mr-1" %>
        <%# sortable @projects, :group, "Group", class: "text-gray-700 mr-1" %>
        <%# sortable @projects, :number, "Number", class: "text-gray-700 mr-1" %>
        <%# sortable @projects, :name, "Name", class: "text-gray-700 mr-1" %>
        <%# sortable @projects, :code, "Code", class: "text-gray-700 mr-1" %>
        <%# sortable @projects, :description, "Description", class: "text-gray-700 mr-1" %>
        <%# sortable @projects, :start, "Start", class: "text-gray-700 mr-1" %>
        <%# sortable @projects, :end, "End", class: "text-gray-700 mr-1" %>
        <%# sortable @projects, :duration, "Duration", class: "text-gray-700 mr-1" %>
        <%# sortable @projects, :routine, "Routine", class: "text-gray-700 mr-1" %>
        <%# sortable @projects, :delivery, "Delivery", class: "text-gray-700 mr-1" %>
        <%# sortable @projects, :scale, "Scale", class: "text-gray-700 mr-1" %>
        <%# sortable @projects, :sorting, "Sorting", class: "text-gray-700 mr-1" %>
        <%# sortable @projects, :status, "Status", class: "text-gray-700 mr-1" %>
        <%# sortable @projects, :percentage, "Percentage", class: "text-gray-700 mr-1" %>
        <%# sortable @projects, :manager, "Manager", class: "text-gray-700 mr-1" %>
        <%# sortable @projects, :supervisor, "Supervisor", class: "text-gray-700 mr-1" %>
        <%# sortable @projects, :account_id, "Account", class: "text-gray-700 mr-1" %>
      </div> -->
    </div>

    <%= link_to t("shared.scaffold.new.title", model: "Project"), new_project_path, class: "btn btn-white" %>
  </div>

  <%= tag.div id: ("projects" if first_page?), class: "bg-white rounded-md shadow p-6 space-y-8" do %>
    <%= render partial: "projects/index", collection: @projects, as: :project, cached: true %>

    <div class="hidden only:flex flex-col items-center justify-between lg:flex-row">
      <%= image_tag "empty.svg", class: "mb-4 lg:w-1/2" %>
      <div class="flex-1 text-center">
        <p class="mb-4 h3">Create your first Project</p>
        <%= link_to t("shared.scaffold.new.title", model: "Project"), new_project_path, class: "btn btn-primary" %>
      </div>
    </div>
  <% end %>

  <% if @pagy.pages > 1 %>
    <div class="mx-1 my-6 text-center">
      <%== pagy_nav(@pagy) %>
    </div>
    <% end %>
</div>
