<%= content_for :title, "Position Types" %>
<%= turbo_stream_from :position_types %>

<div class="container px-4 mx-auto my-8">
  <div class="flex items-center justify-between mb-4">
    <div>
      <h1 class="h3">Position Types</h1>

      <div class="text-sm font-medium">
        <span class="text-gray-500">Sort by:</span>
        <%= sortable @position_types, :identificator, "Identificator", class: "text-gray-700 mr-1" %>
        <%= sortable @position_types, :name, "Name", class: "text-gray-700 mr-1" %>
        <%= sortable @position_types, :description, "Description", class: "text-gray-700 mr-1" %>
        <%= sortable @position_types, :value, "Value", class: "text-gray-700 mr-1" %>
        <%= sortable @position_types, :account_id, "Account", class: "text-gray-700 mr-1" %>
      </div>
    </div>

    <%= link_to t("shared.scaffold.new.title", model: "Position Type"), new_position_type_path, class: "btn btn-white" %>
  </div>

  <%= tag.div id: ("position_types" if first_page?), class: "bg-white rounded-md shadow p-6 space-y-8" do %>
    <%= render partial: "position_types/index", collection: @position_types, as: :position_type, cached: true %>

    <div class="hidden only:flex flex-col items-center justify-between lg:flex-row">
      <%= image_tag "empty.svg", class: "mb-4 lg:w-1/2" %>
      <div class="flex-1 text-center">
        <p class="mb-4 h3">Create your first Position Type</p>
        <%= link_to t("shared.scaffold.new.title", model: "Position Type"), new_position_type_path, class: "btn btn-primary" %>
      </div>
    </div>
  <% end %>

  <% if @pagy.pages > 1 %>
    <div class="my-6 text-center">
      <%== pagy_nav(@pagy) %>
    </div>
  <% end %>
</div>
